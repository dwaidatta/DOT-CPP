#include <bits/stdc++.h>
using namespace std;

int main() {
    // STACK -> LIFO (last in, first out)
    // PUSH POP TOP functions

    stack<int> s;
    s.push(1);
    s.push(2);
    s.push(3);

    s.pop();
    s.top();

    s.size();
    // s1.swap(s2);

    // ALL operations in stack -> O(1) time complexity
}