#include <bits/stdc++.h>
using namespace std;

int main(){
    deque<int> dq;
    /*
        SIMILAR FUNCTIONS

        dq.pushback(ele);
        dq.emplace_back(ele);

        dq.push_front(ele);
        dq.emplace_front(ele);

        dq.pop_back();
        dq.pop_front();

        dq.back();
        dq.front();

        - rest functions same as vectors
    */

    return 0;
}