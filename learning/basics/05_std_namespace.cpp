#include <iostream>

int main () {
    // using the std namespace

    using namespace std;

    // so we can write std::cout -> cout

    cout << "Hello";

    /*
        we can also

        using namespace std::string
        using namespace std::cout

        and omit the required parts
    */

    return 0;
}